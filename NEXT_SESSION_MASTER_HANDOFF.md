# Nemotron Unrestricted — authoritative successor-session handoff

## Authority, project path, and how to use this file

This is the current continuation contract for:

`/data/data/com.termux/files/home/nemotron-unrestricted-app`

It supersedes `NEW_SESSION_MASTER_HANDOFF.md`, which is retained as historical evidence of the earlier checkpoint. Read this file completely before changing source, processes, packages, Windows state, Git state, or user data. The latest user message remains the primary mission. This handoff supplies full context and safety constraints; it is not permission to ignore a newer mission, replay completed work, publish anything, or perform destructive operations.

At the start of a new session:

1. Read this entire file and `/data/data/com.termux/files/home/.codex/memory/lessons.md`.
2. Read the applicable `AGENTS.md` and the `michNvidiaApp Current Setup` preservation skill.
3. Create a visible plan with at least 20 concrete steps as required by the current `AGENTS.md`, with exactly one step in progress.
4. Update that same plan after every material command/result, keeping exactly one step in progress.
5. Complete read-only revalidation before mutation. Preserve newer valid state rather than forcing it back to the snapshot recorded here.
6. Merge the newest user mission into that plan. Do not recreate completed work or infer a publication mission.

Do not stop merely because work is long or a first strategy fails. Use bounded alternatives, preserve progress, and continue until the applicable mission and verification gates are complete, the user explicitly stops, or a real external authorization/platform/hardware gate remains. A detailed plan is a visibility and continuity contract, not authorization to bypass safety, consent, credentials, Android protections, provider limits, or a GitHub publication gate.

## Final white-GUI and cleanup completion — authoritative 2026-07-22 state

This section supersedes every older local v1.1.0 APK hash, frontend hash, test count, UI-acceptance statement, and unfinished white-GUI checkpoint later in this file.

- The final physical-instance failure is fixed at its proven module-graph root cause. `index.html` loaded a renamed content-addressed copy while five lazy chunks still imported canonical `index-BjdL8GKN.js`. Opening Skills/Automations therefore created a second Vue runtime; the global mount guard prevented a second root mount but could not make components from two Vue runtimes compatible. The real `MainActivity` logged the resulting `refs` and `emitsOptions` null dereferences.
- `index.html`, AutomationsPanel, DirectoryHub, ReviewPane, ThreadConversation, and ThreadTerminalPanel now all resolve through canonical `assets/index-BjdL8GKN.js`. The patch/build/test pipelines reject a renamed split entry. The idempotent boot guard remains as defense in depth, and Android acceptance requires `bundleExecutions:1` after exercising a lazy route.
- Production-safe null handling is restored for Vue host sibling, parent, and remove operations. No `NemotronNullSibling`, `NemotronNullParent`, or `NemotronNullRemove` diagnostic exception remains in source, deployed bundle, or APK.
- The final canonical frontend is `assets/index-BjdL8GKN.js`, SHA-256 `9ac45b3c20011ae3d8ac9829db49e9b260f3117e98e413f270cbf166527272c3`. Live-served bytes match exactly. The service-worker cache generation is `codexweb-shell-v6`.
- The old floating `Refresh`, `Force-clean idle threads`, `+ Project`, `− Project`, readiness badge, and provider-receipt UI are removed. The final real-Android proof found `floatingControls:0`.
- Settings contains exactly one visible cleanup card and exactly one red button labeled `Delete all sessions and threads now`. It is immediate one-click operation: there is no confirmation dialog, typed phrase, second button press, or keyboard requirement.
- Cleanup inventories archived and unarchived chats, excludes active and approval-bearing work, creates and verifies a full-history IndexedDB backup before mutation, deletes eligible inactive sessions/threads, verifies deletion and protected-session survival, and fails closed on backup, deletion, or verification errors.
- Cleanup never targets projects, project files, Git data, plugins, skills, accounts, settings, models, selected-model preferences, memories, project automations, running work, or approval state. It removes only thread-scoped browser state after deletion; project order, collapsed projects, model selection, and sidebar preferences remain intact.
- Android verification never opened or foregrounded `MainActivity`. A shell-only verifier attaches a WebView to a private `OWN_CONTENT_ONLY | PRESENTATION` virtual display, renders the real live runtime, opens Settings only inside that private document, loads the signed cleanup overlay, and destroys the presentation afterward.
- Final Android acceptance passed on private display 24 after deliberately opening the lazy-loaded Skills route. It reported `ready:true`, one mounted app child, 22,235 HTML bytes, 4,740 text characters, canonical bundle loaded, `bundleExecutions:1`, `lazyRouteExercised:true`, Settings present, exactly one cleanup card/button, exact button text, 283×257 positive geometry, `floatingControls:0`, zero startup errors, and zero console errors. `physicalDisplay:false`.
- Samsung Launcher remained the verified physical foreground throughout final replacement and proof. No tap, Activity launch, force-stop, uninstall, `pm clear`, or data reset occurred.
- Final APK: `/data/data/com.termux/files/home/nemotron-unrestricted-app/dist/Nemotron-Unrestricted-1.1.0.apk`, 267,726 bytes, SHA-256 `ebf0ad6e32a60e8ac4d96d0c709fcd355359f0363c1eab2c4b96581f34c6f543`.
- The installed `base.apk` independently hashes to the exact same `ebf0ad6e...6f543` bytes. Streamed `pm install -r -S 267726` returned `Success`; first install time remains `2026-07-20 00:36:04`.
- APK v1/v2/v3 signatures and zip alignment verify. Signer certificate SHA-256 remains `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`.
- The exact installed process at final proof was PID 21411. Its logs contained zero fatal/renderer/JavaScript/ANR signatures. Samsung/Adreno WebView capability and shader messages are non-fatal driver output; Android proof recorded zero console errors.
- Full final regression passed 227/227 tests in 42.234 seconds. Source validation passed for 34 Python, 27 shell, and 2 JavaScript files; the cleanup/overlay harness, three off-device Android UI goldens, secret scans, APK scans, signer checks, and `git diff --check` passed.
- Runtime remains healthy on GUI 5904, proxy 18776, and supervisor 18777 with `activeTurnCount:0`. The runtime identity receipt remains unverified for the selected requested model, so it must not be overclaimed as a provider-confirmed execution.
- Android package identity remains `com.michaelovsky.nemotronunrestricted.isolated`, versionName 1.1.0, versionCode 2, minSdk 23, targetSdk 28. Protected config SHA-256 remains `77388729a11c38247701639cb6fbdb3d8c9ddea163ef56544ce3f094ea081c9c`.
- Fresh pre-fix preservation manifest `build/preservation-before-canonical-module-fix-20260722.sha256` verifies after both replacements. It retains one live session/JSONL, one unique thread, and one unique project root with unchanged fingerprints, readable SQLite, and zero parse errors; goals, logs, memories, provider/settings files, projects, and other useful state remain present.
- The current worktree contains intentional uncommitted recovery changes. Do not reset, clean, publish, or overwrite them without a new explicit user request.

## Historical white-GUI recovery checkpoint — superseded 2026-07-22 state

This older checkpoint is retained only as diagnostic history. The final completion section immediately above supersedes it.

- Newest completed mission: repair the all-white Android GUI, remove the floating autonomy controls/receipt, and make the settings cleanup a one-click bulk deletion of inactive chat sessions/threads while preserving active or approval-bearing work and all useful non-session data.
- User constraint honored: the Android app was never opened, tapped, foregrounded, or switched to during diagnosis, implementation, validation, or installation. No force-stop, `pm clear`, uninstall, or data reset was used.
- Fresh exact-PID evidence from the broken build showed the primary bundle throwing `TypeError: Cannot read properties of null (reading 'nextSibling')` before the native overlay loader ran. This disproved the earlier overlay-only diagnosis.
- Separate defects were also confirmed: duplicate `onPageFinished` processing, a synthetic overlay path returning HTML instead of JavaScript, and `@JavascriptInterface` callbacks reading `WebView.getUrl()` from the JavaBridge thread.
- The maintained frontend patch now makes the DOM primitive null-safe: `nextSibling:e=>e==null?null:e.nextSibling`.
- The deployed content-addressed bundle is `assets/index-5113bcc51eee6f65.js`, SHA-256 `5113bcc51eee6f65fa529999ba2d4bb6772e4d79ba6ff1b6ef8297fb602c6ce4`.
- The signed native overlay route is the real static JavaScript URL `/nemotron-autonomy-progress.js`, version `4.9.1`, SHA-256 `b378817cc91df115ce161fda94a561c931a9f9fe44e1daff73042e8658bb5b06`.
- `MainActivity` now single-flights page-finished work per navigation generation, moves all JavaBridge-originating WebView access to the main thread, records startup errors, probes the mounted root after 1.5 seconds, performs one bounded cache-busted root recovery, and renders a dark terminal recovery page instead of an indefinite white screen if that one retry also fails.
- The service worker uses `codexweb-shell-v4` and does not cache/intercept the native overlay route.
- The chat screen has no floating autonomy controls or provider receipt. Regression tests require their absence.
- The settings cleanup is one click with no confirmation dialog or typed phrase. It inventories inactive sessions, creates and verifies a full-history backup before mutation, deletes only inactive sessions/threads, preserves active and approval-bearing work, verifies the post-delete inventory, and fails closed on backup/deletion/verification errors.
- Cleanup does not target projects, project files, plugins, skills, accounts, settings, models, memories, project automations, running work, or approval state.
- The empty preservation manifest verifier was corrected so zero protected entries is a valid verified state rather than a false mismatch.
- Full automated release gate passed on isolated ports `15903/15904/15905`: 226/226 tests, source validation, off-device Android UI goldens, overlay harness, secret scan, isolation preflight, APK build, signer/signature audit, and both preservation checks.
- Current signed APK: `/data/data/com.termux/files/home/nemotron-unrestricted-app/dist/Nemotron-Unrestricted-1.1.0.apk`.
- Current APK size: 263,638 bytes.
- Current APK SHA-256: `1b06d06c8809a963c63d988f06f1ffa57d6e1f26337561980d76ba4a195ab105`.
- Signer certificate SHA-256 remains `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`; v1/v2/v3 schemes verified true and the v4 sidecar is present.
- Package Manager streamed replacement (`pm install -r -S 263638`) returned `Success` at 2026-07-22 19:22:07 UTC. Installed `base.apk` independently read back with the exact same SHA-256.
- Installed identity: package `com.michaelovsky.nemotronunrestricted.isolated`, versionName `1.1.0`, versionCode `2`, minSdk 23, targetSdk 28.
- Android preserved `ceDataInode=689803` and `deDataInode=689806`; first install time remains 2026-07-20 00:36:04.
- Protected config SHA-256 remains `77388729a11c38247701639cb6fbdb3d8c9ddea163ef56544ce3f094ea081c9c`.
- Protected global-state SHA-256 remains `fcb89184dad6de9c5e54044c53b77e869f6618685156ac3983f10bc5705ceeb8`.
- Current preservation readback: zero session files, zero session JSONL files, zero workspace files, zero unique threads, zero unique project roots, SQLite readable, zero parse errors. This was the verified pre-install state and remained identical post-install.
- Package replacement produced only `MY_PACKAGE_REPLACED` -> background runtime-service startup -> Termux `RUN_COMMAND` dispatch. Exact new PID logs contained no fatal exception.
- Current background process PID at checkpoint: `23304` (historical; re-query). The service is a foreground service; Android reported zero Nemotron window matches.
- Foreground remained Samsung Recents during the pre/post-install checks; do not interpret a retained recent-task `ActivityRecord` as a visible window.
- Current dynamic runtime ports: GUI 5904, proxy 18776, supervisor 18777.
- GUI root, corrected bundle, and overlay each returned HTTP 200 with the exact hashes above. Proxy `/v1/runtime-selection` returned HTTP 200 with `status:ok`, `app:nemotron-unrestricted`, and `activeTurnCount:0`. Supervisor `/health` returned HTTP 200 with `status:ok` and `activeTurnCount:0`.
- Off-device headless Edge with Android viewport/user-agent mounted exactly one Vue root after the fix. This is strong shell/render evidence but is not mislabeled as a real Android foreground visual check; the user explicitly prohibited opening the app.
- Current worktree contains intentional uncommitted recovery changes. Preserve and inspect them; do not reset, clean, publish, or overwrite them.

## Exact resume checkpoint

Snapshot verified on 2026-07-21 after the prior implementation mission completed:

- Original 100-step implementation mission: steps 1–99 are complete and locally verified.
- Original step 100 is complete. The user explicitly authorized GitHub publication; a credential-free clean public history was pushed, release v1.0.0 was published, and its APK was independently downloaded and verified byte-for-byte.
- Git remote `origin` is `https://github.com/Michaelunkai/nemotron-unrestricted-android.git`.
- Public repository: `https://github.com/Michaelunkai/nemotron-unrestricted-android`.
- Release: `https://github.com/Michaelunkai/nemotron-unrestricted-android/releases/tag/v1.0.0`.
- Direct APK: `https://github.com/Michaelunkai/nemotron-unrestricted-android/releases/download/v1.0.0/Nemotron-Unrestricted-1.0.0.apk`.
- The current source, APK, installed package, isolated runtime, and exact Dolphin X1 paired-PC route are complete and operating.
- The final Android app was launched and verified in the previous session. Do not open or foreground it merely for orientation. Reopen only when a new mission needs UI validation and the device is not actively being used by the user.
- The successor handoff is committed locally and published in the clean public history. Preserve it and update it after material release changes.

If a new user mission accompanies this handoff, perform that mission on top of the verified baseline. Do not rerun the completed 85 GB model download, rebuild/reinstall without a reason, replay old turns, or restart healthy services just to make progress counters move.

## Project purpose and honest scope

Nemotron Unrestricted is an Android AI-agent application built as an isolated Codex-compatible runtime and WebView shell. Its goal is reliable, tool-first autonomy for lawful user-authorized work: coding, research, web/API access, verified downloads and APK installation, Android UI/app automation, gallery operations, Git/GitHub work, durable long-running tasks, multimodal input/output, paired-Windows-PC work, and authorized defensive security testing.

“Unrestricted” means the app should avoid arbitrary app-added refusals, fake progress, generic capability disclaimers, unnecessary manual handoffs, and silent model substitution when an installed tool can lawfully perform the action. It does not authorize malware, credential theft, unauthorized intrusion, covert surveillance, CAPTCHA/authentication bypass, destructive attacks, protected-attribute inference, or actions outside the user's authority. Provider policy, Android security, account authentication, quotas, physical presence, hardware limits, and network availability remain real boundaries and must be reported honestly.

The APK is not a self-contained 405B model. It uses the isolated Termux runtime, private provider configuration, and the paired Windows PC over Tailscale for exact Dolphin X1.

## Repository and release identity

- Project root: `/data/data/com.termux/files/home/nemotron-unrestricted-app`
- Local Git branch: `agent/fix-nemotron-runtime-recovery`
- Last implementation commit: `cdb35520bd61f29687d5d73a1f585d9076c0af9d` (`Complete Nemotron autonomy and Dolphin X1 recovery`).
- Local publication documentation commit: `c1b2be7` (`Publish Nemotron Unrestricted Android handoff`); a later final-evidence commit follows it.
- Initial public clean-history `main` commit: `f3b3f371dcbfeb7a38fac40539768a2500cb2394`.
- Commit timestamp: `2026-07-21T23:15:02+03:00`
- Worktree is expected to be clean after the final publication-evidence commit.
- Git remote `origin`: `https://github.com/Michaelunkai/nemotron-unrestricted-android.git`.
- Authorized public repository: `https://github.com/Michaelunkai/nemotron-unrestricted-android`.
- Repository discovery rejected the similarly named `Michaelunkai/nemotron-unbound-android` because it is a different package (`com.michaelovsky.nemotron.unbound`), provider architecture, signer, and APK.
- Publication was explicitly authorized by the user on 2026-07-21. The repository was created public so anyone can access the source and release APK.
- GitHub tooling: `./bin/codex-github status` returned authenticated, gatewayVerified, ghAvailable, gitAvailable, ok, and verified all true. Never print or document tokens.
- Publishing gate: satisfied for `Michaelunkai/nemotron-unrestricted-android` by the user's explicit instruction. Do not redirect future pushes/releases to a different repository without new authorization.
- Android package: `com.michaelovsky.nemotronunrestricted.isolated`
- App/version: Nemotron Unrestricted 1.1.0, versionCode 2
- Android SDK: minSdk 23, targetSdk 28
- Final APK: `/data/data/com.termux/files/home/nemotron-unrestricted-app/dist/Nemotron-Unrestricted-1.1.0.apk`
- APK size: 263,638 bytes
- APK SHA-256: `1b06d06c8809a963c63d988f06f1ffa57d6e1f26337561980d76ba4a195ab105`
- APK idsig: present; re-hash the current sidecar rather than using the historical 1.0.0 digest.
- Signer certificate SHA-256: `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`
- APK signatures: v1, v2, and v3 verified true.

## Architecture and important files

- `android/app/src/main/java/com/michaelovsky/nemotronunrestricted/MainActivity.java`: WebView host, lifecycle handling, and recovery without turn replay.
- `android/app/src/main/java/com/michaelovsky/nemotronunrestricted/NemotronRuntimeService.java`: foreground watchdog and bounded runtime recovery.
- `android/app/src/main/java/com/michaelovsky/nemotronunrestricted/BootReceiver.java`: boot/package-replacement restoration without duplicate work.
- `nemotron-unrestricted-start.sh`: isolated HOME/CODEX_HOME/workspace, locks, dynamic port allocation, process ownership, environment, GUI, proxy, and supervisor startup.
- `nemotron_unrestricted_proxy.py`: OpenAI-compatible routing, provider/model truth, request normalization, tool-call repair, retries, streaming, exact-Dolphin health gating, and no-substitution enforcement.
- `nemotron_session_supervisor.py`: durable events, active-turn/completion tracking, notifications, and recovery state.
- `web/nemotron-autonomy-progress.js`: event-driven human-readable progress overlay.
- `isolation-preflight.sh`: collision and isolation validation.
- `verify-nemotron-preservation.sh`: credential-free manifest and session-state verifier.
- `validate-nemotron-sources.sh`: source/catalog consistency gate.
- `bin/`: capability wrappers for Android, Shizuku, packages, gallery, web, download/install, GitHub, Windows, and scoped defensive workflows.
- `windows/setup-dolphin-x1-server.ps1`: paired-PC installation/firewall/scheduled-task setup.
- `windows/start-dolphin-x1-server.ps1`: exact-model llama.cpp startup wrapper.
- `tests/`: Python and JS regression/acceptance coverage.
- `build/release-evidence/`: preservation and release evidence.
- `dist/`: final signed APK and checksum sidecars.

## Protected state and non-destructive rules

Preserve all user projects, conversations, memories, credentials, runtime data, package data, and intentional changes. Never run `git reset --hard`, broad `git checkout`, `git clean`, recursive deletion against a broad/unresolved path, package uninstall, `pm clear`, `.codex` deletion/replacement, session deletion, or app-data clearing. Do not kill sibling app servers or other isolated apps.

Preservation baseline:

- Manifest: `build/release-evidence/preservation-before-dolphin-x1-install.sha256`
- Last stable manifest verification: 57 entries, 57 session files, 57 JSONL files, 57 unique threads, 55 unique project roots, SQLite readable, zero parse errors.
- Stable thread fingerprint: `895a446d103f6276d5cde127`
- Stable project fingerprint: `1c22821729325b2c4ade5dce`
- During creation of this handoff, the static checksum manifest differed only at the active current-session JSONL because Codex was legitimately appending to it.
- Live structural readback then showed 59 session files, 59 JSONL files, 59 unique threads, 57 unique project roots, and zero parse errors. Increased counts and a changed fingerprint are expected from new sessions; regression, disappearance, unreadable SQLite, or parse errors require non-destructive investigation.

Never “repair” an active session file back to an older checksum. For future verification, first identify exactly which manifest entries changed. Active/new session JSONL growth is legitimate; missing files, truncated history, decreased counts, changed old immutable files, or parse errors are not.

Secrets must remain private. Never print, copy, commit, screenshot, embed in APK/docs/tests, rotate, or replace the configured OpenRouter key or any GitHub/gateway credential. Credential presence may be verified only through boolean/fingerprint evidence that reveals no value.

## Android installed-package state

- Installed for Android user 0: true.
- Version: 1.1.0, versionCode 2.
- minSdk 23, targetSdk 28.
- Enabled, not hidden, not suspended, not quarantined.
- Package data existed at the last check; first install time remained 2026-07-20 and update time was 2026-07-21 23:13:45 +03:00.
- Runtime permissions included notifications, Termux RUN_COMMAND, camera, and microphone as previously granted.
- Installed package/signature and final APK were independently read back during acceptance; do not trust installer-wrapper success alone.
- If reinstall is genuinely required, preserve data with replacement semantics, stream exact bytes through `pm install -r -S` or stage under a verified `/data/local/tmp` path when Android cannot read Termux/FUSE paths, then compare installed `base.apk`, version, signer, and package identity.
- Android UI dumps can be stale or belong to another foreground app. Verify `topResumedActivity`/current package and the dump root before interpreting screenshots or XML. The user may be using the phone; avoid stealing foreground unnecessarily.

## Active isolated runtime state

Ports are dynamic and must be read from `runtime/.codex/supervisor/ports.env`, never assumed. At this snapshot:

- GUI: 5904
- proxy: 18776
- supervisor: 18777
- Proxy process at the completed restart: PID/PGID 24724; supervisor PID/PGID 24701; GUI session leader PGID 24735. These PIDs are historical and must be re-queried.
- Proxy `/vault-health`: HTTP 200, exactDolphinAvailable true, exact model `dphn/Dolphin-X1-Llama-3.1-405B`, exact provider `paired-pc-llama.cpp`, `modelSubstitution:false`.
- Proxy source hash: `023d78b58409d992f397657e058c11b082cebef788604ed7087cb5c98d30e04d`
- Supervisor `/health`: HTTP 200, activeTurnCount 0 at snapshot.
- Supervisor source hash: `d30e372bc6bd575a7fbb0b4ac49d12539412acdf33549b5a9fb78c17c30f73b1`
- Runtime methods verified include config/read, thread/start, turn/start, turn/interrupt, and thread/delete.

Ordinary launcher use must reuse a healthy owned runtime. An explicit restart is allowed only after verifying exact process ownership, zero active turns, and the correct session leaders. Kill only owned process groups. Never use broad name matching that can kill sibling services.

Lifecycle recovery must never create a replacement turn, replay a completed turn, or blindly interrupt. It may persist state, reconnect streams, rehydrate the exact thread/turn, and reconcile authoritative status. Progress must be specific, event-driven English from the first observable action, updated after material results and within 60 seconds during long work; timer-generated generic text is not progress. The completion sound fires once only at genuine terminal completion.

## Model truth and routing

- Default hosted model: `nousresearch/hermes-4-405b`.
- Selectable alternate: `cognitivecomputations/dolphin-mistral-24b-venice-edition`.
- Exact paired-PC model: `dphn/Dolphin-X1-Llama-3.1-405B`.
- The originally requested literal identifier `cognitivecomputations/Dolphin-3.0-Llama-3.1-405B` does not exist in checked authoritative catalogs and must never be advertised or silently mapped to another model.
- Exact Dolphin X1 is advertised only while its live health probe succeeds. Requested and effective model identity must remain truthful, with `modelSubstitution:false`.
- Tool-bearing requests must use a route that actually supports the required tool semantics; after the first tool result, return tool choice to auto to avoid loops.
- Reject pseudo tool calls unless they match an advertised, schema-valid tool. Protocol debris such as a lone `</tool_call>` is a recoverable provider failure, not completion.

Exact Dolphin X1 details:

- Model parameters: 405,853,651,008.
- Quantization: IQ1_S, a severe low-bit compromise required by available hardware.
- Runtime context: 4096; training context metadata: 131072.
- Merged GGUF: `E:\AI\Dolphin-X1-405B\Dolphin-X1-Llama-3.1-405B.i1-IQ1_S.gguf`
- Merged size: 85,233,526,624 bytes.
- Part 1 size/hash: 42,949,672,960 / `2387a10ceb3f236c5525985bafef1e6c8430ce3f7a32d980a413121941179e84`
- Part 2 size/hash: 42,283,853,664 / `a6d0cde87f086f20f48d5ad85860807cd8bc57c5fa2f7b534a6434a33ec6bd83`
- Paired endpoint: `http://100.65.146.122:18780`
- Snapshot live checks: `/health` HTTP 200 and `/v1/models` HTTP 200 with the exact alias, 405,853,651,008 parameters, IQ1_S, and context 4096.
- Measured minimal exact-model latency is slow because of 405B partial offload: direct non-streaming about 588 s, direct streaming about 260 s, and proxy non-streaming about 309 s. This is a real external limitation, not a hang by itself.

## Paired Windows PC state

- Computer: `USER`; gateway user `user`.
- Tailscale address: `100.65.146.122`.
- Windows gateway port: 18767; elevated status true.
- `codex-win {"action":"status"}` returned the expected gateway/companion markers. Its outer status may say `completed_unverified`; state-changing actions remain fail-closed and require independent readback.
- Hardware: about 100.55 GB RAM and NVIDIA RTX 5080 with 16,303 MiB VRAM.
- llama.cpp: `E:\AI\Dolphin-X1-405B\llama\llama-server.exe`.
- Scheduled task: `Nemotron-Dolphin-X1-405B`, enabled/running at last acceptance, logon trigger, restart count 999, two-minute restart interval, unlimited execution, IgnoreNew.
- Server arguments include exact alias, Tailscale host, port 18780, context 4096, parallel 1, threads 8, batch 128, ubatch 64, fit enabled, fit target 2048, Q4_0 K/V, flash attention, Jinja, metrics, and no Web UI.
- Firewall rule: `Nemotron Dolphin X1 405B (Tailscale only)`, inbound allow TCP 18780, local `100.65.146.122/32`, remote `100.64.0.0/10`, all profiles. Never expose it publicly.
- Remote setup-script hash: `2bea45c160af798e34875890240f3d1b71760b53b8863b82fc89a6f1c9864018`.
- Remote start-script hash: `5f207eee2dd0de8d3f129b813a6da132c84e7516480a9fb9a78bb47e02db4202`.
- Last healthy resource evidence was about 15,488 MiB VRAM used and about 40.27 GB free RAM. Re-query instead of hard-coding process IDs or resource values.
- Serialize Windows gateway operations with `/data/data/com.termux/files/home/.codex/windows-gateway.lock`. Never run parallel Windows mutations.

## Completed acceptance evidence

Latest 2026-07-22 white-GUI recovery acceptance: 226/226 tests passed twice through the full release flow; `NEMOTRON_RELEASE_GATE_OK`, source validation, secret scan, isolation preflight, signed APK build/audit, off-device Android UI goldens, exact live asset hashes, in-place installed-byte readback, protected-state preservation, and no-window/no-fatal background verification all passed. The older evidence below is historical for the prior release.

- Full Python regression suite: 174/174 passed.
- Lifecycle-focused suite: 62/62 passed.
- Broad acceptance-focused suite: 82/82 passed.
- Source validator: Python 29, shell 27, JavaScript 2, and progress-overlay harness passed.
- JavaScript progress-overlay harness passed after changing the human checkpoint from 90 seconds to 55 seconds and synchronizing the vendor asset/test.
- Current-tree/history/APK secret scans passed: worktree 94 files, history 0 findings, APK 15 entries checked.
- Isolation preflight passed.
- APK build, package/version/signer/signature checks, and data-preserving installation passed.
- No Android crash or ANR was found.
- Exact-model health gating and listing tests passed.
- Installed runtime endpoints returned HTTP 200.
- Harmless chat tests passed on Hermes, Dolphin Mistral, and exact Dolphin X1. Exact responses reported the exact model and returned `OK` without substitution.
- Hermes proxy chat completed in about 0.542 s; Dolphin Mistral in about 15.206 s; Hermes stream reconnect completed in about 0.28 s with three events and DONE.

These are accepted baseline results. Re-run only the gates proportional to a new change. A documentation-only read does not justify rebuilding/reinstalling or rerunning the 405B download.

## Known failures and corrected operating rules

1. A system `rg` binary may fail with `cannot execute: required file not found`. Use the working launcher/project path or bounded `find`/`grep`; do not claim files are absent.
2. `codex-github` may not be globally discoverable. Use `./bin/codex-github` from this project.
3. Direct `codex-fetch` can expose compressed bytes on some sites; use a normalized/Jina fallback and verify content.
4. `codex-delete --help` may treat `--help` as a target. Never infer CLI behavior from that call and never target broad paths.
5. Hugging Face Xet relaunch truncated partial files. Use `HF_HUB_DISABLE_XET=1` for HTTP Range resume and verify byte monotonicity. The model is now complete; do not download it again.
6. Windows foreground hash/copy calls can hard-time-out near 300 seconds. Use one detached bounded worker, an atomic receipt, and poll it under the gateway lock.
7. PowerShell `Get-Content -Raw` serialized huge content poorly; use `[IO.File]::ReadAllText` for exact large text.
8. NetSecurity CIM cmdlets failed with `0x80041010 Invalid class`; the verified fallback is `netsh advfirewall` plus full verbose readback.
9. PowerShell `*>>` made the scheduled llama wrapper exit 1. Use `Start-Process -Wait` with separate stdout/stderr logs.
10. Stopping an 80+ GB llama process can take time. Wait until the exact old PID is absent before starting another instance.
11. Android dumps may be stale and from another app. Verify the resumed/root package before reading or presenting them.
12. The correct focused unittest class is `ProxyNormalizationTests`; full discovery is authoritative when selectors fail.
13. Isolation preflight can transiently fail its 1.5 s endpoint identity check. Verify the exact endpoint and permit one bounded rerun only.
14. A transient Wi-Fi regression passed isolated and on one bounded full-suite retry; never hide the initial result, but final full suite is green.
15. Package/install wrappers may report success without replacing bytes. Always verify package-manager version, signer, installed hash, data inode/preservation, and process state.
16. `pm install -r` may fail from Termux-private/FUSE paths. Use exact-byte stdin streaming or verified `/data/local/tmp` staging, then remove the staging copy.
17. A healthy old process does not prove edited source is active. Compare source hashes after an owned restart.
18. Never treat installation as proof of entitlement, authentication, permissions, model identity, or functional completion.
19. Do not fabricate availability for an absent model or substitute silently. Health, listing, response metadata, and requested/effective identity must all agree.
20. The user can change the foreground app within milliseconds. Reject mismatched screenshots and avoid fighting the user for focus.
21. Do not attribute a main-bundle startup exception to a later overlay event. Collect a fresh exact-PID timeline and compare the first bundle error, loader start, injection, and lifecycle callbacks in timestamp order.
22. A synthetic JavaScript path can be swallowed by SPA fallback and return HTML. Use a real static JS URL, intercept only the exact trusted runtime URI, set no-store headers, and verify MIME/body bytes.
23. Every `@JavascriptInterface` callback runs off the UI thread. Post all WebView reads/writes, including `getUrl()`, to the main handler before access.
24. Duplicate `onPageFinished` callbacks must be single-flight per navigation generation. Blank-root recovery must be bounded to one retry and must never loop, clear user data, or replay a turn.
25. An empty checksum manifest is a valid verified preservation state. Do not pass an empty file blindly to `sha256sum -c`; explicitly validate zero entries and continue with structural state checks.
26. The release gate inherits dynamic runtime ports. When the live owned runtime is intentionally left running, set isolated free gate ports instead of stopping the runtime or accepting a port-collision failure.
27. On this Android build, `cmd package has-signing-certificate` is unavailable. Exact installed-APK byte identity with a locally `apksigner`-verified artifact is authoritative signer evidence.

## New mission routing and completion rules

The newest user instruction always determines productive work:

- If the user provides a new coding, testing, Android, model, or Windows mission with this file, inspect the verified baseline and implement that mission without replaying completed work.
- If the mission overlaps changed files, inspect diffs first and preserve user-owned edits.
- If the user asks only for status/explanation, stay read-only.
- If no mission is supplied, report the latest local checkpoint only. The old publication decision is complete; do not publish the uncommitted 1.1.0 recovery or create a new release without a new explicit instruction.
- A user instruction to “continue,” “finish,” or “do not stop” requires persistence but does not authorize destructive actions, credentials exposure, public release, repository creation, payments, bypasses, or work outside the stated scope.
- Complete each applicable plan step. Conditional/no-op steps may be marked complete with exact evidence explaining why they did not require mutation.

## Historical 100-step continuation plan

The list below is retained as historical context from the earlier mission. Do not recreate it verbatim. Follow the current `AGENTS.md`: build a mission-specific visible plan with at least 20 concrete steps, exactly one in progress, and only unfinished work represented.

1. Read this successor handoff completely before other project actions.
2. Read the accumulated launcher lessons and apply every relevant corrected rule.
3. Read applicable AGENTS instructions and the current-setup preservation contract.
4. Confirm the exact project root without changing it.
5. Create this exact 100-step visible plan with only step 1 initially in progress.
6. Record the newest user mission verbatim and distinguish it from historical work.
7. Confirm whether the mission is read-only, diagnostic, implementation, UI, Windows, or publishing work.
8. Identify any authority, external-state, destructive, credential, payment, or publication boundaries.
9. Record Git branch, HEAD, status, diff summary, and remotes read-only.
10. Preserve this handoff and all unexpected/newer user-owned worktree changes.
11. Confirm the recorded commit lineage still contains `cdb3552`.
12. Verify no remote, push, or release appeared without explicit approval.
13. Verify GitHub tool authentication without displaying credentials.
14. Inspect the preservation manifest and identify changed entries without restoring them.
15. Recount live sessions, JSONL files, threads, project roots, SQLite status, and parse errors.
16. Explain legitimate active-session growth separately from loss or corruption.
17. Verify the final APK and checksum sidecar still agree.
18. Verify APK package, version, signer, and signature schemes read-only.
19. Verify the installed Android package identity and preserved data state if relevant.
20. Read dynamic GUI, proxy, and supervisor ports from the runtime ports file.
21. Query proxy vault health and compare its source hash with the current source.
22. Query supervisor health and require zero active turns before any restart.
23. Verify exact Dolphin health gating and `modelSubstitution:false`.
24. Query the paired PC `/health` and `/v1/models` only if model state matters.
25. Verify Windows gateway identity and Tailscale scope only if Windows work matters.
26. Inspect current logs for errors directly relevant to the newest mission.
27. Map the mission to architecture files, tests, packages, processes, and endpoints.
28. Inspect all overlapping diffs before editing any file.
29. Establish bounded acceptance criteria that prove the requested outcome.
30. Tell the user the exact first productive action and observed baseline.
31. Inspect the primary source/configuration surface for the newest mission.
32. Inspect the first dependent component that consumes that surface.
33. Inspect the second dependent component that consumes that surface.
34. Inspect relevant persistence, lifecycle, or recovery behavior.
35. Inspect relevant UI/progress behavior and accessibility implications.
36. Inspect relevant model/provider/tool routing behavior.
37. Inspect relevant Android permission/package constraints.
38. Inspect relevant Windows/network constraints when applicable.
39. Inspect existing unit tests covering the requested behavior.
40. Inspect existing integration/acceptance tests covering the requested behavior.
41. Define the smallest safe implementation slice for the mission.
42. Implement the first source change with `apply_patch`.
43. Review the first change for accidental scope expansion.
44. Implement the next required source change with `apply_patch`.
45. Synchronize any duplicated/generated/vendor copy required by repository conventions.
46. Update capability/catalog metadata only for behavior backed by a real implementation.
47. Add or update a focused regression test for the first changed behavior.
48. Add or update a focused regression test for the second changed behavior.
49. Add lifecycle/recovery coverage when the mission can survive interruption.
50. Add model-truth/no-substitution coverage when routing is affected.
51. Add exact-target/destructive-safety coverage when files/packages/data are affected.
52. Run Python syntax checks on changed Python files.
53. Run shell syntax checks on changed shell files.
54. Run Java/Android compilation checks when Android sources changed.
55. Run PowerShell parse/static checks when Windows scripts changed.
56. Run JavaScript syntax or harness checks when Web UI/progress changed.
57. Run the narrowest focused test for the first change.
58. Fix any focused-test failure at its root cause.
59. Rerun the focused test and capture the exact pass result.
60. Run the next relevant focused test group.
61. Fix any second-order regression without discarding user changes.
62. Verify secrets and credentials were not added to source, logs, or fixtures.
63. Verify capability claims still match real commands/endpoints/permissions.
64. Verify lifecycle recovery cannot replay or duplicate side effects.
65. Verify progress is factual, event-driven, specific, and timely.
66. Verify model requested/effective identity remains truthful.
67. Verify dynamic ports and sibling-runtime isolation remain intact.
68. Verify destructive actions use exact targets and recoverable methods where possible.
69. Run the relevant broader Python regression suite.
70. Run the progress-overlay harness when UI/progress code is in scope.
71. Run `validate-nemotron-sources.sh` when source/catalog state changed.
72. Run isolation preflight when launcher/runtime/package state changed.
73. Review the complete diff for correctness and unrelated changes.
74. Reconcile documentation with the implemented and verified result.
75. Confirm every newest-mission acceptance criterion now has evidence.
76. Decide whether an APK rebuild is actually required by the changes.
77. If required, build the APK without launching the app.
78. If built, verify package, version, signer, permissions, components, and checksums.
79. If built, run current-tree/history/APK secret scans.
80. Decide whether data-preserving installation is required for end-to-end proof.
81. If required, capture preservation evidence immediately before installation.
82. If required, install with exact-byte replacement semantics and no data clear.
83. If installed, independently compare installed package/version/signer/APK hash.
84. If installed, verify session/project counts and data inode did not regress.
85. Decide whether an isolated-runtime restart is required by changed backend source.
86. If required, re-query exact owned process groups and active-turn count.
87. If safe and required, restart only the isolated Nemotron runtime.
88. Verify new dynamic ports, source hashes, proxy health, and supervisor health.
89. Decide whether paired-PC mutation is required; keep read-only if it is not.
90. If required and authorized, serialize Windows work under the gateway lock.
91. Verify exact Dolphin `/health`, `/v1/models`, firewall scope, task, logs, and resources.
92. Run harmless non-streaming and streaming model checks proportional to routing changes.
93. Verify no silent substitution and record real latency/external limitations.
94. Decide whether Android UI launch is required and safe while the user uses the device.
95. If required, launch once and verify current package before UI evidence.
96. Run the smallest end-to-end user flow proving the newest mission.
97. Recheck crashes, ANRs, endpoint errors, preservation, secrets, and worktree state.
98. Commit only intentional local changes if the mission includes or clearly requires a commit.
99. Present exact completed results, tests, artifacts, and remaining honest limitations.
100. If and only if explicit publishing approval plus a private destination exist, configure the remote, push, create/upload the APK release, independently download/hash it, and report the verified URL; otherwise stop at the publication gate with all local work preserved.

## Safe orientation commands

Run individually or in bounded read-only groups; never combine them with destructive actions:

```bash
cd /data/data/com.termux/files/home/nemotron-unrestricted-app
sed -n '1,520p' NEXT_SESSION_MASTER_HANDOFF.md
git branch --show-current
git rev-parse HEAD
git status --short
git remote -v
cat runtime/.codex/supervisor/ports.env
./bin/codex-github status
sha256sum dist/Nemotron-Unrestricted-1.0.0.apk
```

For preservation, do not treat a changed active JSONL checksum as automatic corruption. First list only failed manifest entries, then run structural counting and parse validation. For runtime health, source the live ports file and query the exact ports. For installed-package evidence, account for Shizuku/rish stdout/stderr wrapping and independently read back package-manager state.

## Final publication contract

GitHub publication is complete and public:

- Repository: `https://github.com/Michaelunkai/nemotron-unrestricted-android`
- Default branch: `main`
- Initial credential-free public commit: `f3b3f371dcbfeb7a38fac40539768a2500cb2394`
- Release/tag: `v1.0.0`
- Release ID: `357632216`
- APK asset ID: `485145253`; size 42,160 bytes
- APK URL: `https://github.com/Michaelunkai/nemotron-unrestricted-android/releases/download/v1.0.0/Nemotron-Unrestricted-1.0.0.apk`
- Checksum URL: `https://github.com/Michaelunkai/nemotron-unrestricted-android/releases/download/v1.0.0/Nemotron-Unrestricted-1.0.0.apk.sha256`
- idsig URL: `https://github.com/Michaelunkai/nemotron-unrestricted-android/releases/download/v1.0.0/Nemotron-Unrestricted-1.0.0.apk.idsig`

## 2026-07-22 local v1.1.0 autonomy upgrade (completed, not published)

This section supersedes older local-runtime/build facts above, but it does not alter the published v1.0.0 release facts. The user did not request publication.

### Final installed artifact

- Package: `com.michaelovsky.nemotronunrestricted.isolated`
- Version: `1.1.0`, versionCode `2`, minSdk 23, targetSdk 28.
- APK: `/data/data/com.termux/files/home/nemotron-unrestricted-app/dist/Nemotron-Unrestricted-1.1.0.apk`
- Final SHA-256: `a1c7bdb75040906b0462d085a513c123ef3a15c7b0a9d301137621d0f12b622c`.
- Signer SHA-256 remains `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`.
- Verified v1/v2/v3 signatures, v4 sidecar, zip alignment, exact installed `base.apk` bytes, permissions, components, and unchanged first-install timestamp.
- Exact-byte `pm install --user 0 -r -S` replacement was used. No uninstall or data clear occurred.
- Preservation passed before and after install: 65 session JSONLs, 65 unique threads, 63 project roots, zero parse errors, identical fingerprints.

### Implemented capabilities

- Network/download: deterministic Termux CA environment plus the global downloader as primary, followed only on failure by three bounded resumable curl strategies. Redirects remain HTTP(S), output stays confined, credentials are never printed, final URL/status/size/hash are verified, and failed integrity removes partial/output files.
- Exact runtime identity: `bin/codex-runtime-status` reports requested and effective provider/model/effort from authoritative metadata. It must be run before answering identity questions.
- Exact NVIDIA model: `nvidia/nemotron-3-ultra-550b-a55b` is exposed only while present in the live provider catalog. Live non-stream and stream requests returned that exact model without substitution.
- Max effort: composer includes `Max`; provider transport maps it truthfully to `reasoning.max_tokens=128000`, not to an invented native enum. Requested/effective values remain distinct until a provider continuation actually uses the change.
- Mid-turn switching: model/effort updates apply to the next provider continuation of the one matching active thread. Existing session, turn, tool history, manual-stop state, and already generated tokens are preserved.
- Fail closed: an unavailable exact model, unsupported capability, effort conflict, or provider-reported model substitution fails before response delivery.
- Paired PC: status, diagnostics, and strict read-only PowerShell receipts retry three times; mutations remain one-shot/upstream-verified. A narrowly parsed `netsh advfirewall firewall show rule name=...` form is read-only; add/delete/set remain blocked.
- Android automation: typed JSON intents cover activity, broadcast, foreground service, and resolve-only routes with bounded fields, shell quoting, typed extras, URI/MIME validation, and postconditions. Existing Shizuku/UI/app/package/Termux API surfaces remain protected.
- Bulk cleanup: the screenshot-style red settings card says `Clean sessions and threads` / `Delete all sessions and threads`. It inventories archived/unarchived threads, excludes active and approval-bearing threads, requires the exact typed phrase `DELETE ALL SESSIONS AND THREADS`, backs up every eligible thread to IndexedDB before deletion, stops on the first failure, verifies deletion/protected preservation, and never touches projects/files/plugins/skills/accounts/settings/models/memories/automations.
- Background: START_STICKY foreground service, boot/package-replace recovery, `onTaskRemoved` dispatch, durable thread reconciliation, and a non-reference-counted PARTIAL_WAKE_LOCK held only while authoritative `activeTurnCount > 0`.
- Terminal ringtone: completed/failed/stopped outcomes generate one dedicated six-note 48 kHz PCM ringtone with exactly 144,000 samples (3 seconds) and AudioTrack gain `0.5f`. Event-triggered delivery is primary, 250 ms polling is recovery, a synchronized durable sequence prevents overlap/duplicates, and acknowledgements record waveform/duration/volume metadata.
- A live acceptance event proved wake-lock acquire/release and ringtone acknowledgement sequence 2. The first static-buffer implementation failed and retried; it was replaced by bounded `MODE_STREAM` playback, rebuilt, reinstalled, and verified end to end.

### Final live state and evidence

- Dynamic owned ports after final restart: GUI `5905`, proxy `18774`, supervisor `18775`; always re-read `runtime/.codex/supervisor/ports.env` rather than hard-code.
- Proxy/supervisor source hashes match their current source files, health is `ok`, activeTurnCount is 0, terminal sequence and acknowledgement are both 2.
- Exact Nemotron live checks: non-stream 24.865 s, stream 0.988 s for tiny low-effort/8-token requests; both reported `nvidia/nemotron-3-ultra-550b-a55b` exactly.
- Requested runtime selection is exact Nemotron + Max. Last effective request was exact Nemotron + Low; with no active turn this distinction is intentional and truthful.
- Dolphin PC endpoint remains `100.65.146.122:18780`; exact alias, 405,853,651,008 parameters, IQ1_S, n_ctx 4096. Task is running, listener is Tailscale-only, firewall local `100.65.146.122/32` remote `100.64.0.0/10`, server log/process healthy.
- Focused suite passed 137/137 before the final audio fix. All 182 discovered tests passed in bounded module batches. After the final fix, 19 native/supervisor/policy tests, real Java compilation, source validation, build secret scan, and live ringtone/wake-lock acceptance passed.
- Source validator: Python 30, shell 27, JavaScript 2, progress-overlay harness, overall validation passed.
- Current-tree/APK scan passes. The private reachable Git history still has 22 pre-existing legacy OpenRouter/signing-material findings; do not publish this branch or rewrite history without explicit authorization. The previously published clean v1.0.0 history is separate.
- Android exit-info has no crash/ANR for this acceptance; expected reasons are package updates, one explicit corrective force-stop, and WebView isolated-process self-exits.

### 2026-07-22 no-launch capability-verification addendum

- The live checks above are preserved as historical acceptance evidence; they are not proof that the exact route or credential remains usable at a later time.
- Exact Nemotron is now exposed only when three fresh checks agree: the exact ID is present in the live OpenRouter catalog, the configured credential reports usable nonzero capacity through `/api/v1/key`, and the public exact-model endpoint catalog reports a healthy `Together` route supporting `reasoning`, `max_tokens`, `tools`, and `tool_choice`.
- Capability results are bounded, fail closed, cached for at most 60 seconds, and keyed by the private credential-source fingerprint. Replacing or updating the credential source triggers re-verification automatically. Neither the key nor account/billing response data is retained in the public model/status payload.
- At this verification, the configured private credential reported `limit=0` and `limit_remaining=0`; a direct exact-model probe returned a sanitized HTTP 403 key-limit rejection. The exact model and Max are therefore intentionally unavailable until credential capacity changes, while unrelated models and existing fallbacks retain their prior behavior.
- A provider response for any direct or UI-selected exact request must identify both the exact model and `Together`; missing or substituted identity fails before response delivery. Authoritative 401/402/403 exact-route failures immediately invalidate the cached capability with a sanitized actionable reason.
- Fresh off-device artifacts were built: release SHA-256 `6462fa96759cfebef0d3e1a2b0511e6e75d1aaeebf7ce183f2c080927ae19efc`; debug SHA-256 `c1dc49ca021c4f495026c02917bc0b0b0693bd849ef5607c701502d5ce35da31`. Both are 259,534 bytes, aligned, v1/v2/v3 signed by the expected certificate, secret-scan clean, and contain runtime-contract files byte-for-byte equal to maintained sources. Only `AndroidManifest.xml` and signing metadata differ; only the debug APK is debuggable.
- The final discovered regression suite passed 215/215. Focused proxy/capability tests passed 125/125 and endpoint/status/supervisor/cleanup/native/off-device-golden tests passed 81/81. Source validation passed for 34 Python, 27 shell, and 2 JavaScript files plus deterministic four-state 945×2048 goldens and the cleanup DOM harness.
- After the user reported that the cleanup button was absent in the actual Nemotron app, installed-package readback proved that version 1.1.0 still used the older `a1c7bdb75040906b0462d085a513c123ef3a15c7b0a9d301137621d0f12b622c` APK, which had 15 ZIP entries, no `assets/runtime-contract/` directory, and none of the three cleanup tokens. The live Nemotron server already served the current versioned overlay with `Cache-Control: public, max-age=0` and all cleanup strings.
- The verified release was then streamed once through `pm install -r -S 259534` and returned `Success`; it was never foreground-launched. Independent readback now hashes installed `base.apk` to `6462fa96759cfebef0d3e1a2b0511e6e75d1aaeebf7ce183f2c080927ae19efc`, verifies signer SHA-256 `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`, and confirms three packaged runtime-contract entries plus `Clean sessions and threads`, `Delete all sessions and threads`, and `DELETE ALL SESSIONS AND THREADS`.
- The in-place update preserved first-install time `2026-07-20 00:36:04`, CE/DE data inodes `689803`/`689806`, granted notification/camera/microphone permissions, 67 session JSONLs, 67 unique threads, 65 project roots, both preservation fingerprints, readable SQLite, and zero parse errors. No uninstall, clear-data, force-stop, launch intent, UI automation, instrumentation, or device screenshot was used. The separate `com.michaelovsky.codexnvidia.isolated` reference app retained its July 21 update time and data inode and was not modified.

### 2026-07-22 real cleanup-card placement correction

- This section supersedes the earlier inference that packaged cleanup strings alone proved the button would be visible. The user's real Nemotron screenshot at 14:57 showed that inference was false: the settings panel ended at Context/build information with no cleanup card.
- The actual bundled Vue DOM places Telegram, optional `.sidebar-settings-context-row`, `.sidebar-settings-rate-limits`, and terminal `.sidebar-settings-build-label` children inside `.sidebar-settings-panel`. The overlay used `panel.appendChild(card)`, which placed the cleanup card after the terminal build footer where the real scroll/clipping layout hid it.
- `web/nemotron-autonomy-progress.js` now resolves the first real anchor among Context, rate limits, and build label and calls `panel.insertBefore(card, insertionPoint)`. It appends only when none exists. Placement stays scoped to `.sidebar-settings-panel` and remains idempotent across repeated calls and settings-panel remounts.
- `tests/progress_overlay_harness.js` now models the real Telegram/Context/rate-limit/build-label order. Its fail-first assertion reproduced the defect (`cleanup card must be directly before Context`, observed index 5 instead of 1). The corrected implementation passes direct-before-Context placement, exactly-one-card idempotency, remount, rate-limit fallback, build-label fallback, accessibility, and click-wiring checks.
- The synchronized live server on GUI port `5904` served `nemotron-autonomy-progress.js?v=9ff677240a78555a`; its bytes equal the maintained overlay SHA-256 `9ff677240a78555a9046f8abf85eb1c75a50e1e621812a346c7cca828905b7da` and contain the corrected selector plus `insertBefore` logic.
- Final release APK SHA-256 is `4d9b77ce9a744f409485cb37575d8a5f5eb50c1798049e0cf2c89963901d87a2`. It is 259,534 bytes, ZIP-aligned, v1/v2/v3 signed by certificate SHA-256 `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`, secret-scan clean, and embeds the corrected overlay byte-for-byte.
- Silent exact-byte `pm install -r -S 259534` returned `Success`. Installed `base.apk` independently hashes to the same `4d9b77ce...87a2`; version remains 1.1.0/code 2; first-install time remains `2026-07-20 00:36:04`; CE/DE inodes remain `689803`/`689806`; notification/camera/microphone permissions remain granted.
- Fresh manifest `build/preservation-before-real-cleanup-ui-install-20260722.sha256` verified all 67 session records before and after replacement. `activeTurnCount` remained zero. No uninstall, data clear, app launch, foreground switch, tap, swipe, instrumentation, or device screenshot occurred.
- Final verification passed 215 discovered tests, the realistic DOM harness, source validation (34 Python, 27 shell, 2 JavaScript), four deterministic 945x2048 off-device UI states, diff-format checks, the canonical secret scan, and a final artifact audit reporting exactly one cleanup button with `device_interaction=false`. `build/offdevice-ui/settings.png` is the no-launch visual proof generated from the maintained UI contract.

### 2026-07-22 unobstructed chat and one-click cleanup correction

- The user's real screenshots showed that overlay 4.7.0 finally loaded, but its fixed `Refresh`, `Force-clean idle threads`, `+ Project`, `- Project`, `Ready`, and runtime-receipt surfaces obstructed the chat. Those floating surfaces are now prohibited. Internal progress/recovery state remains functional, while `removeFloatingUi()` removes both legacy injected roots and no code creates either root.
- The cleanup interaction is now exactly one click from the Settings card. There is no modal, typed phrase, confirmation input, focus trap, keyboard confirmation, or Cancel step. The button reads `Delete all sessions and threads now` and dispatches the guarded cleanup operation immediately.
- Safety remains inside the operation rather than in user friction: a verified full-history IndexedDB backup completes before the first deletion; active/running and approval-bearing sessions are excluded; deletion stops on the first failure; the backup is retained; and a fresh inventory must prove every eligible session/thread absent and every protected one present. Projects, project files, plugins, skills, accounts, settings, models, memories, and project automations are never cleanup targets.
- Overlay/API/native loader version is `4.8.0`. Canonical, packaged, APK-embedded, and live-served overlay SHA-256 is `cf8eb9d40481502ad7fdd02fe3ddba428c6f3726a6a4be7f86297a664b3f0232`; the live cache tag is `cf8eb9d40481502a`.
- Release APK SHA-256 is `cf08750f733db3c20ad3c0dcf1ba7acb3ae4862941898b6bae417cc835f36e17`, v1/v2/v3 signed by unchanged certificate `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`. Silent streamed `pm install -r` returned `Success`, and installed `base.apk` independently matches the release hash.
- The executable browserless cleanup harness passed, 37 focused tests passed, all three deterministic 945×2048 off-device states matched, and the full suite passed 218/218. Source validation passed for 34 Python, 27 shell, and 2 JavaScript files; diff hygiene and secret scans passed.
- Installation did not launch, tap, switch to, or otherwise operate Nemotron. A different launcher package remained foreground. All 67 session JSONLs remained byte-for-byte unchanged across installation with aggregate fingerprint `3521713f2ab775d40c0ffb287d9d3ca441e0c5bcb0cd35553a523bd6344b3994`.

### 2026-07-22 cleanup sidebar-convergence correction

- The user's 16:18 screenshots showed `0 deleted` while the sidebar still displayed many Pinned and Chats rows. Read-only live evidence after that click showed both archived and unarchived `thread/list` inventories at zero and no session JSONLs in the target session directories; before the click there were 67. The deletion backend had completed, but the visible sidebar was stale.
- Root cause: the production frontend's forced thread refresh calls `gs(Ht, groups)` once the store is initialized. That function merges new rows into cached groups; an authoritative empty response therefore cannot remove cached rows. The overlay's previous `nemotron-autonomy:soft-refresh` event had no production listener. A Settings remount could also discard the successful receipt, allowing a later click to display a misleading zero-result card.
- Cleanup now emits `nemotron-autonomy:sessions-deleted` with exact deleted IDs, protected IDs, and an authoritative `clearAllThreads` flag. The real frontend registers `nemotronCleanupConverge`, removes those IDs through its own `fo()` state mutation, performs a forced authoritative refetch, and repeats removal afterward so an older in-flight response cannot resurrect a deleted row. Empty authoritative inventory clears every cached Pinned/Chats row without reloading or destroying an active route.
- The last cleanup receipt is retained across Settings-panel remounts. A repeated click against an already empty authoritative store resynchronizes the sidebar and keeps the previous successful deletion receipt instead of overwriting it with `0 deleted`.
- Overlay/native-loader version is `4.9.0`. Canonical, packaged, APK-embedded, and live overlay SHA-256 is `aa5962d662dfbe1c1d99bae7b7c7324af450454219d420ac5b42d87ef4b44b4a`; live cache tag is `aa5962d662dfbe1c`. Packaged/APK-embedded/live frontend SHA-256 is `5d528707da959c6eab7eaf04d0fa1137c8cd82d80fe0021cedd8e0486bb0b1e7`.
- Release/installed APK SHA-256 is `62c62afb074a3fcaaf63481dd77312068e0803a0fed30f2e2ea5038b225368b3`, v1/v2/v3 signed by unchanged certificate `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`. Streamed `pm install -r` returned `Success`; another launcher remained foreground and Nemotron was not opened or operated.
- Fail-first coverage reproduced the missing convergence event and absent production handler. Final executable coverage verifies exact-ID purge, all-row purge for empty inventory, protection of active/approval sessions, backup/readback, receipt remount, and a stale in-flight refetch attempting to reinsert a deleted row. The full suite passed 218/218; focused 37/37, off-device goldens, source validation, diff hygiene, and secret scans passed.
- Pre/post installation fingerprints match for all 181 useful state files (`61e09e0e...`), workspace-root metadata (`a757a0ca...`), config, and global state. The session store remained authoritatively empty across installation; no cleanup method was invoked by the implementation/install process.

### 2026-07-22 strict-mode convergence crash correction

- The user's next `Not working` report was correct. Read-only `NemotronUiProof` logcat from the real target WebView showed overlay `4.9.0` loaded, followed by `Uncaught ReferenceError: G is not defined` from `assets/index-BjdL8GKN.js`. The session backend was already empty, but this exception aborted the handler that removes its stale Pinned/Chats rows.
- Exact root cause: the minified production bundle is an ES module and therefore strict JavaScript. `nemotronCleanupConverge` assigned `G=()=>{...}` after an array literal without declaring `G`. The extracted Node VM test was accidentally running that handler in sloppy mode, where the undeclared assignment succeeds, so the test hid the Android failure.
- The handler now declares `G` lexically in its function scope before the immediate purge, forced authoritative refetch, and second purge. The executable frontend harness now evaluates the exact extracted production function with `"use strict";`, reproduces the old `ReferenceError`, verifies the immediate purge, simulates an older in-flight response reinserting stale rows, and verifies the second purge removes them again.
- The corrected frontend is content-addressed as `assets/index-2810a0942569869c.js`. `index.html` references that new URL once and the old crashing URL zero times, preventing the service worker or WebView HTTP cache from substituting old bytes. Canonical, generated, APK-embedded, and live-served frontend SHA-256 is `2810a0942569869cb2f30adabb5dd0ca85117990ab2e8aa8dc706e13704d6036`.
- The patch tool derives and atomically publishes the content-addressed name, migrates the undeclared legacy form, rewrites `index.html`, and its check mode verifies canonical/served byte equality and the current HTML reference. Build and APK audits calculate the same name dynamically and reject missing declarations or the buggy undeclared token.
- The corrected full suite passed 219/219. Focused tests passed 38/38; strict executable harness, three off-device goldens, source validation (34 Python, 27 shell, 2 JavaScript), APK runtime-entry inspection, signature verification, and `git diff --check` passed.
- Corrected release and installed `base.apk` SHA-256 is `065f745adeb72af1b30eacb21f9ae306965bdba1dc6cce8fb997f83f39866e91`, v1/v2/v3 signed by the unchanged certificate `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`. Silent streamed `pm install -r` returned `Success`; Nemotron was not opened, tapped, switched to, or data-cleared.
- Post-install live checks show frontend HTTP 200, exact hash `2810a094...`, one declared handler, two cleanup-event registrations, no undeclared handler form, overlay HTTP 200 with unchanged `aa5962d6...` hash, and archived/unarchived `thread/list` counts both zero. All 181 useful-state files, workspace registration, config, and global-state hashes exactly match the pre-install snapshot.

### 2026-07-22 white-screen post-mount recovery

- The user's 16:58 screenshot correctly showed a completely white Nemotron WebView after the strict-handler/content-address release. Read-only logcat from the exact target PID recorded the first fatal application line: `console http://127.0.0.1:5904/assets/index-2810a0942569869c.js:14 TypeError: Cannot read properties of null (reading 'nextSibling')`. The Vue renderer failed before meaningful UI content appeared; protected runtime data was not damaged.
- Two startup-order paths allowed DOM instrumentation to collide with Vue's cold mount. `index.html` executed `nemotron-autonomy-progress.js` before the main module, and Android's `onPageFinished` callback immediately called `ensureSessionCleanupCard()`. Real timestamps showed native loader evaluation at `16:58:17.084` and the Vue `nextSibling` failure only 114 ms later. The newly content-addressed cold bundle exposed this timing defect.
- `index.html` no longer executes the overlay. The overlay file remains deployed and APK-embedded, but the Android activity is now its sole target-app boot path. `sync-nemotron-web.sh`, `isolation-preflight.sh`, and `tools/patch-codexapp-ui.py` remove/reject any pre-mount overlay tag so later syncs cannot restore the race.
- MainActivity schedules the signed intercepted overlay only after `postVisualStateCallback` commits a rendered frame, then waits a bounded 750 ms settling interval and revalidates WebView identity, URL, and load generation before injection. No Android lifecycle recovery starts, interrupts, or replaces a turn.
- The service-worker shell cache moved from `codexweb-shell-v2` to `codexweb-shell-v3`; live HTML has exactly one `#app` mount, exactly one content-addressed module reference, and zero pre-mount overlay references. The main module, CSS, overlay, manifest, and service worker all return HTTP 200; the main module parses as an ES module and both archived/unarchived inventories remain zero.
- Fail-first tests rejected the deployed pre-mount tag and immediate native injection before implementation. Final focused verification passed 75 tests plus `PROGRESS_OVERLAY_HARNESS_OK`; the complete suite passed 220/220, source validation passed for 34 Python, 27 shell, and 2 JavaScript files, three deterministic off-device mobile goldens passed, and diff hygiene passed. The unavailable `agent-browser` command was explicitly attempted and returned command-not-found; no browser or Android UI was opened for synthetic acceptance.
- Corrected release and installed APK SHA-256 is `28433c53d8b8a2f7d13fc5bfbdb353144822a35b21c619c46c3f3a2b0dcd9858`, v1/v2/v3 signed by unchanged certificate `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`. Streamed `pm install -r` returned `Success`; Chrome stayed foreground and Nemotron was not launched, tapped, switched to, or data-cleared.
- Installed data inode remains `689803`. Sessions remain authoritatively empty. All 181 useful-state files retain aggregate `61e09e0e...`; config and global-state hashes exactly match the pre-install snapshot. Projects, files, plugins, skills, accounts, settings, models, memories, and automations were untouched.

### Honest platform limits

### 2026-07-22 signed WebView overlay-loader correction

- The user's real Nemotron screenshot at 15:35 and `NemotronUiProof` telemetry disproved the prior conclusion again: the Settings panel existed, but `overlayVersion` was empty and panel/card/button telemetry was `false/false/false`. Placement was no longer the active failure; the 154 KB APK asset sent directly through one opaque `evaluateJavascript(source, ...)` call never initialized in the real WebView.
- `MainActivity` no longer evaluates the full packaged source string. Its `RuntimeWebViewClient` intercepts the exact trusted-origin path `/__nemotron-native-overlay.js`, serves `assets/runtime-contract/nemotron-autonomy-progress.js` directly from the signed APK as `application/javascript`, and applies `no-store`, `no-cache`, and `nosniff` response headers.
- `onPageFinished` now evaluates only a small cache-busted loader. The loader removes any stale loader element, appends the intercepted same-origin script, calls `ensureSessionCleanupCard()` only after `onload`, and emits explicit `native-loader-loaded`, `native-loader-already-ready`, or `native-loader-error` telemetry. WebView JavaScript console errors are also logged under `NemotronUiProof`.
- Overlay/API version is `4.6.0`. Canonical and deployed overlay SHA-256 is `a05cc324b90f543d9dcc0a812a190f22927dff44638161e29352135ea9b8d9d9`; deployed `index.html` uses cache key `a05cc324b90f543d`.
- Release APK SHA-256 is `62b5233728c985d3af5e174bb7cc54922868580badf5eda68334e5fc6bd6f20a`. The APK is v1/v2/v3 signed by the unchanged certificate `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`; its embedded overlay hash exactly matches the canonical source.
- The complete discovered suite passed 217/217 in 42.158 seconds. Focused native/overlay/off-device tests passed 35/35, `PROGRESS_OVERLAY_HARNESS_OK`, source validation, deterministic UI goldens, secret scans, and `git diff --check` all passed.
- Fresh manifest `build/preservation-before-intercepted-overlay-install-20260722.sha256` recorded all 67 session files. `pm install -r -S 263630` returned `Success`; installed `base.apk` independently hashes to `62b52337...f20a`; all 67 pre-install hashes still verify, file count remains 67, version remains 1.1.0/code 2, and permissions remain unchanged. TikTok stayed foreground; Nemotron was not launched or switched to.
- Do not declare the UI accepted from static/package evidence. The remaining acceptance gate is the user's next natural Nemotron Settings visit: require real telemetry proving overlayVersion `4.6.0`, exactly one card and button, positive width/height, non-`none` display, visible visibility, and matching DOM text. Do not open or switch apps to manufacture this proof.

### 2026-07-22 Chromium bootstrap correction after user rejection

- The user's next rejection led to the first actionable real-WebView console evidence. The normal 4.6.0 overlay did load, then aborted at line 2606 with `TypeError: Cannot assign to read only property 'CONNECTING'`. The wrapper had inherited Chromium's non-writable WebSocket constants through `Object.setPrototypeOf` and then illegally assigned those same constants. The equivalent EventSource loop had the same latent defect.
- The native intercepted fallback separately returned the runtime HTML shell and raised `SyntaxError: Unexpected token '<'`; it never exported the overlay API. This fallback is not accepted as UI evidence. The normal versioned same-origin asset remains the primary path.
- The realistic Node harness now defines WebSocket and EventSource ready-state constants as non-writable/non-configurable, reproduces the exact Chromium exception on the old implementation, and passes after the correction. The wrappers now retain inherited constants and use guarded `Object.defineProperty` only if prototype inheritance is unavailable; direct assignment is forbidden by source-contract tests.
- Corrected overlay/API version is `4.7.0`; canonical, deployed, APK-embedded, and live-served overlay SHA-256 is `019c586e6c23c41d4d6ec350abe9f73e2c0aaf47d0fc74b7bfd7a0996f71ae2c`. The live index uses cache tag `019c586e6c23c41d`.
- Corrected release APK SHA-256 is `c838fc4c9bdeb3b6355e940ffd497d4df16357e24ce9bc9570e075717c412ccb`, v1/v2/v3 signed by the unchanged certificate. `pm install -r -S 263630` returned `Success`; installed base.apk independently matches the release hash.
- The complete suite passed 218/218 in 42.061 seconds; focused tests passed 36/36; source validation, realistic DOM harness, four off-device goldens, diff hygiene, worktree scan, and APK scan passed.
- Fresh manifest `build/preservation-before-websocket-bootstrap-fix-install-20260722.sha256` verified all 67 session files before and after installation. `activeTurnCount` was zero, version/permissions remained unchanged, and TikTok stayed foreground. Nemotron was not launched or switched to.
- Acceptance remains intentionally open until a post-install natural Nemotron Settings visit emits literal overlay version `4.7.0` and real card/button geometry. Ignore the older PID 15535 / 4.6.0 log entries; they are the failure evidence that this build corrects.

- Wi-Fi-off work continues over another available network and local work persists offline, but remote inference/download/PC access cannot occur with every radio unavailable. Power-off and explicit Android force-stop cannot be defeated by an app.
- The foreground service, scoped wake lock, durable ledgers, and reconnect logic maximize continuity but Android/OEM resource policy cannot make a literal zero-error guarantee.
- The PCM buffer is exactly three seconds and gain is 50%; Android scheduling, hardware routing, notification stream volume, mute, and DND prevent a literal same-millisecond/acoustic-volume guarantee.
- Mid-turn selection affects the next provider continuation, never bytes already generated upstream.
- Android authentication, runtime permissions, secure surfaces, and app-specific anti-automation controls remain authoritative; automation fails closed instead of bypassing platform security.
- Exact Dolphin 405B remains extremely slow under partial offload. Do not classify the documented multi-minute latency as a hang without checking process/endpoint evidence.
- GitHub reported APK digest: `sha256:c604f0b456428b9fdf3bb62917789b07d5f3e44e9f5c6772581d20b96cdf0385`.
- Independent Android download was 42,160 bytes, byte-identical to the local APK, and verified with v1/v2/v3 signatures plus signer certificate `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`.

The original local Git history was deliberately not pushed because the required scanner found 22 legacy-history references involving an OpenRouter key and signing-material paths. No history was destroyed or rewritten. A clean one-commit public branch was generated from the current credential-free tree, cloned independently, and passed `SECRET_SCAN_OK worktree=95 history=95`. Future public work must build on the clean GitHub `main` history; never push the private local legacy ancestry to this repository.

Anyone can download and install the APK on compatible Android devices, but the APK is a launcher/lifecycle shell rather than a bundled 85 GB model or complete Termux distribution. Full agent behavior requires the documented Termux runtime, permissions, private local credentials, and optional paired-PC model setup. Never claim that APK installation alone removes those honest requirements.

This handoff is context, safety policy, verified evidence, and a continuation map. It is not a reason to restart completed work. Continue from the newest user mission, or from the explicit publication gate if no newer mission exists.
