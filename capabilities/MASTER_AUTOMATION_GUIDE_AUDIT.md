# Master Automation Guide implementation audit

Audit source: `runtime/home/Documents/Codex/2026-07-23/connect-to-my-pc-and-tell/MASTER_AUTOMATION_GUIDE.md` (334 lines, read in full on 2026-07-23).

This is the completion ledger for every section of the guide. A section is `Delivered` when it has an executable, model-visible route plus focused proof. `Safely corrected` means the requested outcome is delivered through an Android-appropriate mechanism while an unsafe, inaccurate, or unavailable prescription is explicitly refused instead of being advertised as working. Optional external services remain configuration- or consent-gated and must never be represented as locally verified before their own identity/readback check passes.

Every selectable text, vision, and tool-capable model receives the same synchronized developer contract, `AGENTS.md`, capability catalog, and machine-readable matrix. If the selected model cannot emit tool calls, only the tool-bearing continuation is routed through a currently verified tool-capable model; transcript, instructions, attachments, approvals, plan, and English progress state are preserved. The shared contract is SHA-256 identified in every `/models` entry and `/vault-health`.

| Guide section | Disposition | Implemented surface and completion evidence |
|---|---|---|
| 1. Environment and foundations | Delivered | `codex-doctor all|environment|toolchain|freshness|android|performance` reports locale, timezone, project-local roots, shells, storage, utilities, optional runtimes, build systems, package managers, containers, memory, storage, zram, and thermal state without mutation. `codex-storage` manages exact reversible Termux/runtime links. |
| 2. Toolchain and runtimes | Delivered / safely corrected | `codex-doctor toolchain|freshness --versions` inventories Python, Node, JDK, Go, Rust, C/C++, Android tools, Maven/Gradle/Ninja/PHP and optional container/build routes without false availability. `codex-maintain check|plan|apply` makes freshness opt-in, hash-bound, backed up, removal-refusing, serialized, and post-verified instead of performing fragile unattended daily upgrades. |
| 3. Android/Termux optimization | Delivered | `codex-exec` has optional scoped wake-lock acquisition with guaranteed cleanup. `codex-boot` creates one inert, exact-hash-bound Termux:Boot profile for SSH agent or bounded PC monitoring and never creates a permanent wake lock. `codex-battery`, `codex-storage`, `codex-shizuku`, and `codex-doctor performance` provide reversible readiness and live device diagnostics. |
| 4. Permissions and access | Delivered / platform-gated | `codex-doctor android`, `codex-device`, `codex-shizuku`, typed intent metadata, and the Termux API registry distinguish installed commands, hardware, manifest permission, runtime permission, foreground, authentication, and consent gates. Restricted Android permissions, Accessibility, secure surfaces, and Shizuku authorization are never silently granted or bypassed. |
| 5. Automation and scheduling | Delivered | `codex-task validate|dry-run|run|resume|status|cancel` supplies the validated JSON/YAML DSL, English step purpose, mandatory postconditions, safe retries, mutation/consent/rollback metadata, checkpoints, process-group cancellation, and resume. `codex-schedule create|list|status|pause|resume|cancel|run-now` binds tasks to Android JobScheduler with constraints, missed-run policy, receipts, exact IDs, and self-cancelling one-shot jobs. Project-local `codex-job`, `codex-goal`, `codex-recover`, `codex-capability`, `codex-artifact`, and `codex-verify` resolve only allowlisted trusted delegates and retain the isolated Nemotron runtime root. |
| 6. PC bridge | Delivered / live verified | `codex-pc status|diagnostics|query` uses direct JSON argv, typed failure classes, required-output/JSON-field postconditions, and invalidates cached identity on failure. `codex-pc-monitor` provides bounded health checks, consecutive-failure accounting, cooldown alerts, and exact lifecycle. A live paired-PC identity, Tailscale address, gateway port, diagnostics receipt, and read-only `Get-Date` query were verified before release. |
| 7. Retry and circuit breaker | Delivered | `codex-automation` provides shared exponential backoff with jitter, retry-safety declarations, open/half-open/closed circuits, explicit cooldowns, and typed failure recording. Proxy, research, browser, download, install, PC, network, and task routes bound attempts/time and change strategy after repeated identical failure. |
| 8. Cache and persistence | Delivered | `codex-automation cache` provides namespaced SQLite TTL state, atomic writes, expiration, pruning, vacuum, integrity checks, and corruption recovery. `codex-inventory` persists bounded gateway/capability/environment/package records with the guide’s TTL classes and identity-change invalidation. |
| 9. Logging and observability | Delivered | `codex-automation log|increment|gauge` emits recursively redacted correlated JSONL, bounded rotation, counters, gauges, latency/retry/verification metrics, and no prompt or model-output fields. `codex-dashboard` is optional, loopback-only, strict-projection, PID-managed, and exposes health/metrics/safe summaries only. `codex-pc-monitor` supplies thresholded Android alerts. |
| 10. Skills and plugins | Delivered / safely corrected | `codex-registry refresh|status|search` validates skill front matter, plugin IDs/versions/contained paths, capability commands, hashes, limits, and duplicates. Installed skill/plugin-creation and domain skills remain model-visible. Discovery is manifest-only and never imports or executes arbitrary startup hooks or discovered `script.sh` content. |
| 11. Web research and browser | Delivered | `codex-research search|fetch` supplies domain/keyword filters, bounded count/paging, per-domain throttling, retries, `Retry-After`, and source provenance. `codex-browser-safe` performs scoped waits, forms, navigation, downloads, allowlisted evaluation, PNG/PDF evidence, response/artifact hashes, and explicit failure for unsupported JavaScript-only actions; installed browser routes remain verification-gated fallbacks. |
| 12. Download/install | Delivered / safely corrected | `codex-download` confines paths, rejects credentialed URLs/symlink escapes/existing targets, bounds bytes/time/retries, validates redirects, hashes readback, and removes partial failures. `codex-artifacts` provides provenance, quarantine, restore, promotion, rollback, and retention. `codex-install` pins APK checksum/package/signer, blocks downgrades, installs silently, and independently reads back version/hash/signer/permissions. Untrusted post-install scripts and unrestricted global language installers are replaced by reviewed task/pipeline steps with explicit postconditions. |
| 13. Project scaffolding | Delivered | `codex-scaffold list|create|verify|rollback` includes Python, Node, shell, static web, Go, Rust, Java, and PowerShell templates, staging verification, Git initialization, tree hashes, workspace confinement, and changed-tree rollback refusal. `codex-toolchain inspect|verify` supplies fixed-argv or bounded adapters for formatting, syntax, types, tests, and builds without installing dependencies or pretending an optional compiler exists. |
| 14. CI/CD | Delivered / capability-gated | `codex-pipeline plan|run|rollback` implements hash-gated local checks, fixed adapters, English gates, atomic static deployment, complete target readback, backup, and exact rollback. `codex-release` implements increasing semver/code, synchronized Android/build/gate versions, changelog/notes, APK identity/signature/hash metadata, and unchanged-file rollback. GitHub/PC and third-party deployment routes are used only after live capability and account verification; unrooted Docker/`act` is not falsely promised. |
| 15. Android UI/app control | Delivered / consent-gated | `codex-intent` offers typed open URL/app/settings/share/view/capture workflows with resolve-before-execute, content-URI support, redaction, and URI refusal. `codex-ui-safe snapshot|run` is exact-selector first, package-scoped, stale-state/retry bounded, coordinate-fallback explicit, evidence-hashed, and postcondition-mandatory. `codex-android` supplies package/current/dump/screenshot/tap/swipe/text/key routes. Authentication, payments, secure surfaces, and user consent remain explicit gates. |
| 16. Sensors/system APIs | Delivered / hardware-gated | `codex-device` inventories 83 Termux API commands and device features with TTL and hashed identity. `codex-device-io` provides typed clipboard, notification, toast, TTS, media, and volume routes with stdin-only sensitive text, redaction, and readback. The registry covers battery, camera, location, sensors, telephony, SMS, contacts, Wi-Fi, NFC, USB, biometrics, sharing, SAF, media, speech, vibration, torch, wallpaper, and storage while checking hardware/permission/privacy/reversibility per action. `codex-gallery faces|semantic` scans MediaStore in resumable offset pages, narrates exact English progress, retries omitted detector receipts individually, builds checksum-verified contact sheets, and requires inline presentation before completion. Face presence and non-sensitive visible details are supported; identity and sensitive-attribute inference remain deliberately excluded. |
| 17. Networking/fallbacks | Delivered / safely corrected | `codex-netdiag` classifies DNS/TCP/HTTPS/route/Wi-Fi/PC outcomes with latency and actionable errors. `codex-lan-discover` is bounded and private-scope. `codex-pc-route` admits only identity-verified gateway, strict-known-host SSH/SFTP, public-key-pinned HTTPS, or authenticated signing-required SMB, with health, latency, five-minute selection, and 20% hysteresis. Insecure FTP, automatic DNS bypass, and unauthenticated route switching are intentionally excluded. |
| 18. Security/credentials | Delivered / safely corrected | `codex-secrets` uses Android-Keystore-bound AES-256-GCM envelopes, stdin-only values, scopes, executable allowlists, redaction, ciphertext-only rotation, and recoverable delete/restore. `codex-security audit|artifact` checks runtime/package identity, manifests, routes, secret scan, destructive-command denial, modes, artifact integrity, archives, traversal, links, and expansion. `codex-care` encrypts backups. Flash `shred` claims are replaced by encryption, minimal retention, recoverable retirement, and verified key/ciphertext handling. |
| 19. Extensibility and DSL | Delivered / safely corrected | `codex-command register|list|plan|run|status|cancel|rollback` turns validated tasks into reusable versioned personal commands with strict schemas, consent classes, idempotency, verification, cancellation, and reverse rollback. `codex-task` is the JSON/YAML DSL. `codex-registry` provides safe discovery. Arbitrary auto-run pre/post hooks are excluded; hooks must be explicit validated task steps. |
| 20. Utility library | Delivered | `codex-files` supplies protected-root inspection, SHA-256, copy readback, ZIP list/create/extract, traversal/link/overwrite refusal, staging, and per-file verification. Installed jq/YAML, HTTP, archive, compression, database, process, and notification tools are inventoried by `codex-doctor`; higher-risk actions flow through typed tasks. `codex-device-io` handles local clipboard; PC clipboard sync requires an explicit scoped PC task and independent readback rather than a surveillance daemon. |
| 21. Maintenance | Delivered / opt-in | `codex-care status|backup|verify|restore|recovery-drill|retention-plan|retention-apply|upkeep-plan|upkeep-apply|database-check` provides scrypt + AES-256-GCM backups, authenticated per-file verification, isolated restores, drills, exact-hash retention, SQLite pre-backup/integrity/pruning/vacuum, and unchanged-log retirement. `codex-maintain` stages package freshness safely. Destructive recurring upgrades, deletion, or key rotation are never scheduled without an explicit policy. |
| 22. Future-proofing | Delivered / external sources gated | The live model catalog, `/models` capability contract hash, health probes, `codex-registry`, and `codex-doctor freshness` expose upstream changes. `codex-resilience safe-mode|sync-plan|sync-apply|review|health|recover-local-state` supplies exact-hash reversible local-only mode, non-destructive cross-device sync with conflict copies, redacted review, combined health, and cache recovery. Static-tool availability is diagnosed rather than invented. |

## Guide prescriptions corrected for Android, safety, or truthful verification

- Termux uses Android app lifecycle and JobScheduler facilities, not `systemd-resolved` or a general-purpose systemd service manager.
- Docker-based `act`, Docker builds, Bazel, raw block-device operations, kernel/LSM bypasses, and guaranteed raw-packet features are capability-gated and are not advertised on an unrooted phone.
- Android runtime permissions, Accessibility, battery exemptions, Shizuku authorization, authentication, file/share pickers, payments, device lock, and secure surfaces remain user/platform gates.
- FTP is not an automatic fallback because it lacks the identity and confidentiality required for PC automation. Verified gateway, strict SSH/SFTP, pinned HTTPS, or signing-required SMB are used instead.
- `shred` and `wipe` cannot guarantee physical erasure on flash media. Encryption, limited retention, recoverable retirement, and key destruction are the valid controls.
- Daily package upgrades, global language-package updates, and routine SSH-key rotation can break a working environment or invalidate trust. The app reports/stages changes, binds them to an exact plan hash, preserves relevant state, refuses removals, and verifies health.
- Plugin discovery never executes arbitrary code. Custom hooks are explicit task steps with schemas, consent classification, postconditions, and rollback metadata.
- Clipboard, gallery, contacts, SMS, telephony, microphone, camera, location, and UI automation never run as ambient surveillance; each action follows its typed permission/privacy/consent contract.

## Release completion evidence

- Full automated suite: 353 tests passed, including deterministic APK reconstruction, gallery omission recovery/presentation, exact model-switch interruption, restricted-route network fallbacks, background recovery, completion audio, and all existing non-regression coverage.
- Build validation: runtime bootstrap, web asset synchronization, Python/shell/JavaScript syntax, off-device UI goldens, progress-overlay harness, source validation, and worktree/APK secret scans passed.
- Release APK: `Nemotron-Unrestricted-1.6.0.apk`, version code 7, 292402 bytes, SHA-256 `7d0b256ad3baa27247ded34879310ebfa09733df265d351abbf78416c422ac5d`.
- Signing certificate SHA-256: `f9eddd82a7fe4e0ce902f956e35f29dbaea2b7cd97f33f29fa323945a7df528f`; APK Signature Schemes v1, v2, and v3 verified.
- Silent update: source and installed APK hashes/signers matched; version 1.6.0/code 7 read back from code 6 through an exact-size streamed Package Manager session, with no uninstall, data clear, visible installer, or foreground app launch.
- Preservation: five session JSONL files, five thread/project fingerprints, and readable SQLite state remained unchanged with zero parse errors.
- Device/runtime: all 12 managed storage links, both battery exemptions, foreground sticky service, active-turn-only partial wake lock, Shizuku identity, DNS/TCP/HTTPS/route/Wi-Fi, paired-PC identity, exact completion-tone acknowledgement, and gallery presentation verified after installation.
- Private WebView: the maintained `OWN_CONTENT_ONLY` proof returned ready with settings/cleanup UI, one cleanup card, zero floating controls, zero console errors, and `physicalDisplay=false`.
- Model routing: a live request returned the exact requested `nvidia/nemotron-3-ultra-550b-a55b` identifier with no substitution. Provider response metadata proved identity and reasoning use but did not echo an exact effort label, so `effortVerified` correctly remained false.
- Every selectable model receives the synchronized contract requiring factual English progress, durable completion, inline image presentation, and exact active-turn interruption once when model or effort changes.

## Completion rule

A guide item is complete only when its route exists, every eligible model can discover it, permission/consent/failure behavior is truthful, focused tests pass, and an independent readback proves the requested effect. Documentation, an installed binary, an exit code, a generic progress sentence, or a command row alone is not completion evidence.
